apiVersion: v1
kind: Secret
metadata:
  name: nginx-secret           # Name used to reference this Secret in Pods
type: Opaque                   # Generic secret (as opposed to TLS, SSH, etc.)
stringData:                    # Plain text here — Kubernetes base64-encodes it automatically
  DB_PASSWORD: "super-secret-password"
  API_KEY: "abc123xyz"
